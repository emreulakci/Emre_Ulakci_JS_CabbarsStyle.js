# Emre_Ulakci_JS_CabbarsStyle.js
const yil = 7;
const renk = "Mavi";
const marka = "Audi";
const model = "2012";

if (yil > 5) {
    console.log("Hosgeldiniz");
} else if (((marka == BMW) && (renk == Kirmizi) && (model < 2020 && model > 2009)) || (marka == BMW) && (renk == Siyah) && (model == 2020)) {
    console.log("Hosgeldiniz");
} else if ((marka == Audi) && (renk == Kirmizi || renk == Siyah) && ((model > 2004 && model < 2019) || (model > 2013 && model < 2021))) {
    console.log("Hosgeldiniz");
} else if ((marka == VW) && (renk == Siyah) && (model > 2000 && model < 2019)) {
    console.log("Hosgeldiniz");
} else {
    console.log(Uzgunuz);
}
